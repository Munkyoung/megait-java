# 배열 문제

## 로또

```java
	package com.day04.homework;

import java.util.Arrays;
import java.util.Scanner;

public class homework03 {

	public static void main(String[] args) {
	Scanner sc = new Scanner(System.in);
	int[] choose = new int[6];
	int[] correct = new int[6];
	// 사용자 숫자 뽑기
	for (int i = 0; i < choose.length; i++) {
		System.out.println("1부터 45 까지의 숫자 6개를 입력하세요");
		choose[i] = sc.nextInt();
		for (int j = 0; j < i; j++) {
			if (choose[i] == choose[j]) {
				System.out.println("중복된 숫자 입니다 다시 입력해 주세요.");
				--i;
			} // for
		} // for

	} // 사용자가 고른 수 for 문

	for (int i = 0; i < correct.length; ++i) {
		correct[i] = (int) (Math.random() * 45 + 1);
		for (int j = 0; j < i; ++j) {
			if (correct[i] == correct[j]) {
				--i;
			}

		}
	}

	System.out.println(Arrays.toString(choose));
	System.out.println(Arrays.toString(correct));
	int count = 0;
	for (int i = 0; i < choose.length; i++) {
		for (int j = 0; j < choose.length; ++j) {
			if (choose[i] == correct[j]) {
				++count;
			}
		}
	}
	System.out.println("맞은 번호의 수는 : " + count + " 개 입니다.");

	}// main
}
```
